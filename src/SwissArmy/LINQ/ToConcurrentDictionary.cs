namespace SwissArmy.LINQ
{
    public static partial class LinqExtended
    {
        //public static ConcurrentDictionary<TKey, TElement> ToDictionary<TSource, TKey, TElement>(this IEnumerable<TSource> source, Func<TSource, TKey> keySelector, Func<TSource, TElement> elementSelector, IEqualityComparer<TKey> comparer)
        //{
        //    var dictionary = new ConcurrentDictionary<TKey, TElement>(comparer);
        //    foreach (TSource source1 in source)
        //        dictionary.AddOrUpdate();
        //    return dictionary;
        //}
    }
}
